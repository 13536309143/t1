//==============================================================================
// 文件：src/meshlod/meshlod_config.h
// 模块定位：默认构建配置入口，根据目标三角形数量生成一套可用的 clodConfig。
// 数据流：输入是最大三角形约束；输出是可传给 clodBuild 的默认配置。
// 方法说明：默认配置提供实验基线，使不同模型可以在相近 簇 预算下比较 LOD 行为。
// 正确性约束：默认值要满足算法、Scene 和 着色器 三方上限，不能生成 着色器 无法消费的 簇。
// 注释风格：使用中文解释 CPU 侧语义；保留必要的 API、类型名和数学缩写以便检索。
//==============================================================================
#pragma once


// 依赖说明：引入本编译单元需要的外部库、项目模块和共享着色器布局。
// 依赖顺序通常反映抽象层次：先外部库，再项目模块，最后与 GPU 共享的接口定义。
#include "meshlod_types.h"

#ifdef __cplusplus
extern "C"
{
#endif


// 函数：clodDefaultConfig。封装本文件中的一段核心逻辑，保持调用方只依赖清晰的接口语义。
// 输入/输出：输入由参数、成员状态或绑定资源提供；输出通常表现为返回值、成员状态更新、GPU 缓冲写入或命令缓冲记录。
// 设计要点：该函数的主要价值在于隔离局部实现细节，使模块边界和调用顺序更容易审查。
clodConfig clodDefaultConfig(size_t max_triangles);

#ifdef __cplusplus
}
#endif
